pre-commit:
  jobs:
    - name: Format staged files
      run: bunx prettier {staged_files} --write --ignore-unknown --no-error-on-unmatched-pattern
      stage_fixed: true

pre-push:
  jobs:
    - name: Check format
      run: bun run format:check
